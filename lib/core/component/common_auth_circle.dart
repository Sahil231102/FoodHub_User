import 'package:flutter/cupertino.dart';

class CommonAuthCircle extends StatelessWidget {
  const CommonAuthCircle({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [],
    );
  }
}
